# workspace Memory - 2026-03-31
## Shared Knowledge Sync - 2026-03-31

### Lessons Learned
# 经验教训

- 使用 `edit` 工具时，总是先调用 `memory_get` 而直接读文件
- 优先使用 `memory_search` 搜索，搜索范围默认只当前 workspace
- 琜索时指定 `memory/` 可扩大到共享目录

## Lessons Learned
# 经验教训

- 使用 `edit` 工具时，总是先调用 `memory_get` 而直接读文件
- 优先使用 `memory_search` 搜索，搜索范围默认只当前 workspace
- 琜索时指定 `memory/` 可扩大到共享目录

## Lessons Learned
# 经验教训

- 使用 `edit` 工具时，总是先调用 `memory_get` 而直接读文件
- 优先使用 `memory_search` 搜索，搜索范围默认只当前 workspace
- 琜索时指定 `memory/` 可扩大到共享目录

## Lessons Learned
# 经验教训

- 使用 `edit` 工具时，总是先调用 `memory_get` 而直接读文件
- 优先使用 `memory_search` 搜索，搜索范围默认只当前 workspace
- 琜索时指定 `memory/` 可扩大到共享目录

## Lessons Learned
# 经验教训

- 使用 `edit` 工具时，总是先调用 `memory_get` 而直接读文件
- 优先使用 `memory_search` 搜索，搜索范围默认只当前 workspace
- 琜索时指定 `memory/` 可扩大到共享目录

## Lessons Learned
# 经验教训

- 使用 `edit` 工具时，总是先调用 `memory_get` 而直接读文件
- 优先使用 `memory_search` 搜索，搜索范围默认只当前 workspace
- 琜索时指定 `memory/` 可扩大到共享目录

## Lessons Learned
# 经验教训

- 使用 `edit` 工具时，总是先调用 `memory_get` 而直接读文件
- 优先使用 `memory_search` 搜索，搜索范围默认只当前 workspace
- 琜索时指定 `memory/` 可扩大到共享目录

---

## 📚 共享知识同步 (2026-03-31 14:10:57)

**同步内容**:
- wcs-java
  - `shared/project-knowledge/wcs-java/vipc-cleanup-fix-2026-03-31.md`

- 经验教训: `shared/lessons-learned.md`

---

## 📚 共享知识同步 (2026-03-31 14:11:32)

**同步内容**:
- wcs-java
  - `shared/project-knowledge/wcs-java/vipc-cleanup-fix-2026-03-31.md`

- 经验教训: `shared/lessons-learned.md`

---

## 📚 共享知识同步 (2026-03-31 20:16:45)

**同步内容**:
- wcs-java
  - `shared/project-knowledge/wcs-java/vipc-cleanup-fix-2026-03-31.md`

- 经验教训: `shared/lessons-learned.md`
