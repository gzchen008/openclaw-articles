# 让 AI 督促 AI 写代码：OpenClaw 控制 Claude Code 的 5 种方案

> 你说需求，AI 编排 AI 去实现。这才是真正的"躺平式编程"。

---

## 😫 痛点：一个人盯着 Claude Code 太累了

如果你用过 Claude Code，一定遇到过这些情况：

- **任务太复杂**，一个 Agent 撑不住，需要多角色协作
- **想并行开发**，但 Claude Code 只能串行干活
- **需要监督**，但盯着屏幕看它写代码太累
- **质量把控**，改完一堆文件，不知道有没有问题

有没有办法让 **OpenClaw 去编排和监督 Claude Code**？

答案是：**有！而且有 5 种方案。**

---

## 🎯 效果展示：5 种控制方式对比

| 方案 | 核心能力 | 适合场景 | 复杂度 |
|------|----------|----------|--------|
| **cc-godmode** | 8 Agent 编排 + 双质量门 | 严格质量控制 | ⭐⭐⭐⭐ |
| **claude-team** | iTerm2 多窗口 + git worktree | 可视化并行开发 | ⭐⭐⭐ |
| **codex-orchestration** | PTY 后台 + 6 种编排模式 | 轻量灵活 | ⭐⭐ |
| **openclaw-claude-code-skill** | MCP 协议 + 多模型代理 | 深度集成 | ⭐⭐⭐ |
| **coding-agent** | PTY 后台统一接口 | 快速启动 | ⭐ |

下面详细介绍最实用的 3 种。

---

## 方案一：cc-godmode —— 最完善的监督体系

### ✨ 核心亮点

**8 个专业化 Agent，各司其职：**

```
@researcher  → 技术研究、文档查找
@architect   → 架构设计、技术决策
@api-guardian → API 变更影响分析（强制！）
@builder     → 实现代码
@validator   → 代码质量检查
@tester      → UX 质量检查（E2E、性能、A11y）
@scribe      → 文档更新
@github-manager → Issue/PR 管理
```

### 🔄 工作流程

```
                    ┌──▶ @validator ──┐
用户 ──▶ @researcher ──▶ @architect ──▶ @builder ──────────┼──▶ @scribe
                    └──▶ @tester   ──┘
                          (并行!)
```

**亮点：双质量门并行验证**

- @validator：TypeScript 编译 + 单元测试 + 安全检查
- @tester：E2E 测试 + 截图 + 性能指标 + A11y

两个 Agent **同时跑**，比串行快 40%！

### 📦 安装

```bash
npx clawhub@latest install cc-godmode
```

### 🎮 使用示例

```
你：New Feature: 添加用户登录功能

cc-godmode：
→ Working on v1.2.0 - 添加用户登录功能
→ @researcher: 研究最佳登录方案...
→ @architect: 设计 JWT + Session 双模式...
→ @builder: 实现登录 API + 前端组件...
→ @validator + @tester: 并行验证...
→ @scribe: 更新 CHANGELOG
→ ✅ 完成！请 review 后 push
```

---

## 方案二：claude-team —— 可视化并行开发

### ✨ 核心亮点

**在 iTerm2 里看着多个 Claude Code 同时干活！**

```
┌─────────────────────────────────────────────────┐
│  iTerm2 窗口                                     │
│  ┌─────────┬─────────┬─────────┬─────────┐      │
│  │ Groucho │ Harpo   │ Chico   │ Zeppo   │      │
│  │ 登录模块 │ API路由 │ 单元测试 │ 文档    │      │
│  └─────────┴─────────┴─────────┴─────────┘      │
└─────────────────────────────────────────────────┘
```

每个 worker：
- 独立的终端窗口，可随时介入
- 独立的 git worktree（不冲突！）
- 可关联 Issue（自动标记状态）

### 📦 安装

```bash
npx clawhub@latest install claude-team
```

### 🎮 使用示例

```bash
# 启动 3 个 worker 并行处理
mcporter call claude-team.spawn_workers \
  workers='[
    {"project_path": "auto", "bead": "issue-123", "annotation": "登录模块"},
    {"project_path": "auto", "bead": "issue-124", "annotation": "API路由"},
    {"project_path": "auto", "bead": "issue-125", "annotation": "单元测试"}
  ]'

# 等待全部完成
mcporter call claude-team.wait_idle_workers \
  session_ids='["Groucho","Harpo","Chico"]' \
  mode="all"

# 查看日志
mcporter call claude-team.read_worker_logs session_id="Groucho"
```

### ⚠️ 注意

- 只支持 **macOS + iTerm2**
- 需要启用 iTerm2 Python API（Preferences → General → Magic）

---

## 方案三：codex-orchestration —— 轻量灵活编排

### ✨ 核心亮点

**6 种编排模式，按需选用：**

| 模式 | 流程 | 适合 |
|------|------|------|
| 三角评审 | 多 reviewer → 合并 | 多角度审视代码 |
| 评审→修复 | reviewer → implementer → verifier | 清理漏斗 |
| 侦察→行动→验证 | scout → orchestrator → implementer → verifier | 不熟悉的领域 |
| 分片处理 | 每个 worker 一块 → 合并 | 可分割的任务 |
| 研究→综合 | 多 researcher → synthesizer | 技术调研 |
| 选项冲刺 | 多 worker 生成选项 → 选择 | 方向决策 |

### 📦 安装

```bash
npx clawhub@latest install codex-orchestration
```

### 🎮 使用示例

**模式：三角评审**

```bash
# Worker 1：代码正确性审查
codex exec --output-last-message /tmp/w1.txt "
CONTEXT: WORKER
TASK: Review auth.ts for correctness
LENS: 正确性、完整性
"

# Worker 2：安全性审查
codex exec --output-last-message /tmp/w2.txt "
CONTEXT: WORKER
TASK: Review auth.ts for security
LENS: 安全漏洞、注入风险
"

# Worker 3：代码风格审查
codex exec --output-last-message /tmp/w3.txt "
CONTEXT: WORKER
TASK: Review auth.ts for style
LENS: 代码风格、可读性
"

# Orchestrator 合并结果
cat /tmp/w1.txt /tmp/w2.txt /tmp/w3.txt | # 综合分析...
```

---

## ⚠️ 避坑指南

### 1️⃣ cc-godmode 的坑

- **需要 MCP 服务器**：playwright、github 等，配置稍复杂
- **流程较重**：小任务用这个有点杀鸡用牛刀
- **解决**：简单任务直接用 coding-agent

### 2️⃣ claude-team 的坑

- **只支持 macOS**：Linux 用户无缘
- **需要 iTerm2**：其他终端不行
- **解决**：Linux 用户用 codex-orchestration

### 3️⃣ 并行开发的坑

- **多人改同一文件会冲突**
- **解决**：用 git worktree 隔离，或遵循"多读者、单写者"原则

### 4️⃣ 会话丢失的坑

- **后台任务可能被杀**
- **解决**：用 tmux 或 nohup 保护

---

## 🚀 快速上手建议

| 你的情况 | 推荐方案 |
|----------|----------|
| 刚接触，想快速体验 | **coding-agent**（内置，零配置） |
| macOS 用户，想可视化监督 | **claude-team** |
| 需要严格质量控制 | **cc-godmode** |
| 想灵活编排，不折腾 | **codex-orchestration** |

---

## 💡 我的建议

如果你是 **个人开发者**：
> 从 **coding-agent** 开始，需要并行时上 **codex-orchestration**

如果你是 **团队**：
> 强烈推荐 **cc-godmode**，8 Agent 编排 + 双质量门，出活稳

---

## 🔗 相关链接

- OpenClaw 官网：https://openclaw.ai
- ClawHub 技能市场：https://www.clawhub.ai
- cc-godmode 技能：搜索 `cc-godmode`
- claude-team GitHub：https://github.com/Martian-Engineering/claude-team

---

**💬 你会选哪种方案？评论区告诉我！**

---

*作者：小J*
*发布日期：2026-02-15*
